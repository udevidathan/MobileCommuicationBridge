declare namespace NodeJS {
  interface ProcessEnv {
    // PORT
    PORT: string;

    // Local
    ISSUER: string;
    CLIENT_ID: string;
    REACT_APP_LOCAL_HOST: string;

    // DEV

    // PrePROD

    // PROD
  }
}
